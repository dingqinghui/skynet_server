include "config.path"

-- preload = "./examples/preload.lua"	-- run preload.lua before every lua service run
thread = 8
logger = nil
logpath = "."

harbor = 0

start = "testmain"	-- main script
bootstrap = "snlua bootstrap"	-- The service for bootstrap

console_port = 8001

cpath = root.."cservice/?.so"

-- daemon = "./skynet.pid"
-- snax_interface_g = "snax_g"
